name: Build Release
on:
  workflow_dispatch:
    inputs:
      release_type:
        description: "Release type"
        type: choice
        options:
          - dev
          - production
        default: dev
      version_major:
        description: "Major version (production only)"
        type: string
        default: "1"
      version_minor:
        description: "Minor version (production only)"
        type: string
        default: "0"
      version_patch:
        description: "Patch version (production only)"
        type: string
        default: "0"

permissions:
  contents: read

concurrency:
  group: build-release-${{ github.event.inputs.release_type }}
  cancel-in-progress: true

jobs:
  upload_src:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: write
    outputs:
      build_tag: ${{ steps.get_tag.outputs.build_tag }}
      v_major: ${{ steps.get_tag.outputs.v_major }}
      v_minor: ${{ steps.get_tag.outputs.v_minor }}
      v_patch: ${{ steps.get_tag.outputs.v_patch }}
      v_suffix: ${{ steps.get_tag.outputs.v_suffix }}
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit

      - name: Checkout source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ github.sha }}
          fetch-depth: 2
          fetch-tags: true
          submodules: 'recursive'

      - name: Resolve version and create release
        id: get_tag
        shell: bash -l {0}
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TYPE: ${{ github.event.inputs.release_type }}
          INPUT_MAJOR: ${{ github.event.inputs.version_major }}
          INPUT_MINOR: ${{ github.event.inputs.version_minor }}
          INPUT_PATCH: ${{ github.event.inputs.version_patch }}
        run: |
          set -euo pipefail
          if [ "$RELEASE_TYPE" = "production" ]; then
            V_MAJOR="$INPUT_MAJOR"
            V_MINOR="$INPUT_MINOR"
            V_PATCH="$INPUT_PATCH"
            V_SUFFIX=""
            BUILD_TAG="${V_MAJOR}.${V_MINOR}.${V_PATCH}"
            gh release create "$BUILD_TAG" --target "$GITHUB_SHA" --title "Parashell ${BUILD_TAG}" --latest || \
            gh release view "$BUILD_TAG" > /dev/null
          else
            V_MAJOR=$(python3 -c "import json;print(json.load(open('version.json'))['version_major'])")
            V_MINOR=$(python3 -c "import json;print(json.load(open('version.json'))['version_minor'])")
            V_PATCH=$(python3 -c "import json;print(json.load(open('version.json'))['version_patch'])")
            V_SUFFIX="dev"
            BUILD_TAG="weekly-$(date "+%Y.%m.%d")"
            gh release create "$BUILD_TAG" --target "$GITHUB_SHA" --title "Development Build ${BUILD_TAG}" \
                                           -F .github/workflows/weekly-build-notes.md \
                                           --prerelease || \
            gh release view "$BUILD_TAG" > /dev/null
          fi
          echo "BUILD_TAG=${BUILD_TAG}" >> "$GITHUB_ENV"
          echo "build_tag=${BUILD_TAG}" >> "$GITHUB_OUTPUT"
          echo "v_major=${V_MAJOR}" >> "$GITHUB_OUTPUT"
          echo "v_minor=${V_MINOR}" >> "$GITHUB_OUTPUT"
          echo "v_patch=${V_PATCH}" >> "$GITHUB_OUTPUT"
          echo "v_suffix=${V_SUFFIX}" >> "$GITHUB_OUTPUT"

      - name: Apply version to version.json
        shell: bash -l {0}
        env:
          V_MAJOR: ${{ steps.get_tag.outputs.v_major }}
          V_MINOR: ${{ steps.get_tag.outputs.v_minor }}
          V_PATCH: ${{ steps.get_tag.outputs.v_patch }}
          V_SUFFIX: ${{ steps.get_tag.outputs.v_suffix }}
        run: |
          set -euo pipefail
          python3 -c "import json,os; p='version.json'; d=json.load(open(p)); d.update(version_major=int(os.environ['V_MAJOR']), version_minor=int(os.environ['V_MINOR']), version_patch=int(os.environ['V_PATCH']), version_suffix=os.environ['V_SUFFIX']); json.dump(d, open(p,'w'), indent=2); open(p,'a').write('\n')"

      - name: Trigger notes updater workflow (only for weekly)
        if: startsWith(steps.get_tag.outputs.build_tag, 'weekly-')
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0
        env:
          WEEKLY_TAG: ${{ steps.get_tag.outputs.build_tag }}
        with:
          script: |
            const owner = context.repo.owner;
            const repo  = context.repo.repo;

            // Reusable/dispatchable updater workflow file in .github/workflows/
            const workflow_id = 'weekly-compare-link.yml';

            // Use the default branch so the workflow file is available
            const ref = (context.payload?.repository?.default_branch) || 'main';
            const current_tag = process.env.WEEKLY_TAG || '';

            await github.rest.actions.createWorkflowDispatch({
              owner, repo, workflow_id, ref,
              inputs: { current_tag }
            });

            core.info(`Dispatched ${workflow_id} on ${ref} with current_tag='${current_tag}'.`)

      - name: Upload Source
        id: upload_source
        shell: bash -l {0}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python3 package/scripts/write_version_info.py ../freecad_version.txt
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git config user.name 'github-actions[bot]'
          git apply package/disable_git_info.patch
          git commit -a -m "Disable git info write to Version.h"
          git archive HEAD -o parashell_source_${BUILD_TAG}.tar
          git submodule foreach --recursive \
            "git archive HEAD --prefix=\$path/ -o \$sha1.tar && \
             tar -A -f \$toplevel/parashell_source_${BUILD_TAG}.tar \$sha1.tar && \
             rm \$sha1.tar"
          gzip parashell_source_${BUILD_TAG}.tar
          sha256sum parashell_source_${BUILD_TAG}.tar.gz > parashell_source_${BUILD_TAG}.tar.gz-SHA256.txt
          gh release upload --clobber ${BUILD_TAG} "parashell_source_${BUILD_TAG}.tar.gz" "parashell_source_${BUILD_TAG}.tar.gz-SHA256.txt"

  build:
    needs: upload_src
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - { target: linux-64, os: ubuntu-22.04 }
          - { target: osx-arm64, os: macos-latest, deploy_target: "11.0" }
          - { target: osx-arm64, os: macos-latest, deploy_target: "15.0" }
          - { target: win-64, os: windows-2022 }
      fail-fast: false

    runs-on: ${{ matrix.os }}
    environment: weekly-build
    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@bf7454d06d71f1098171f2acdf0cd4708d7b5920 # v2.20.0
        with:
          egress-policy: audit

      # prevent running out of disk space on Ubuntu runners.
      - name: Maximize build space
        if: runner.os == 'Linux'
        uses: AdityaGarg8/remove-unwanted-software@90e01b21170618765a73370fcc3abbd1684a7793 # v5
        with:
          verbose: 'true'
          remove-android: 'true'      # (frees ~9 GB)
          remove-cached-tools: 'true' # (frees ~8.3 GB)

      - name: Set platform environment variables
        shell: bash -l {0}
        env:
          OPERATING_SYSTEM: ${{ runner.os }}
        run: |
          if [[ $OPERATING_SYSTEM == 'Windows' ]]; then
            echo 'PIXI_CACHE_DIR=D:\rattler' >> "$GITHUB_ENV"
            echo 'RATTLER_CACHE_DIR=D:\rattler' >> "$GITHUB_ENV"
          fi

      - name: Checkout source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ github.sha }}
          fetch-depth: 2
          fetch-tags: true
          submodules: 'recursive'

      - name: Apply version to version.json
        shell: bash
        env:
          V_MAJOR: ${{ needs.upload_src.outputs.v_major }}
          V_MINOR: ${{ needs.upload_src.outputs.v_minor }}
          V_PATCH: ${{ needs.upload_src.outputs.v_patch }}
          V_SUFFIX: ${{ needs.upload_src.outputs.v_suffix }}
        run: |
          set -euo pipefail
          python3 -c "import json,os; p='version.json'; d=json.load(open(p)); d.update(version_major=int(os.environ['V_MAJOR']), version_minor=int(os.environ['V_MINOR']), version_patch=int(os.environ['V_PATCH']), version_suffix=os.environ['V_SUFFIX']); json.dump(d, open(p,'w'), indent=2); open(p,'a').write('\n')"

      - uses: prefix-dev/setup-pixi@a09b6247153796b190642a2b53fac4241043cf6f # v0.10.0
        with:
          pixi-version: v0.68.1
          cache: false

      - name: Install the Apple certificate and provisioning profile
        id: macos_get_cert
        if: runner.os == 'macOS'
        env:
          APP_SPECIFIC_PASSWORD: ${{ secrets.APP_SPECIFIC_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          BUILD_CERTIFICATE_BASE64: ${{ secrets.BUILD_CERTIFICATE_BASE64 }}
          BUILD_PROVISION_PROFILE_BASE64: ${{ secrets.BUILD_PROVISION_PROFILE_BASE64 }}
          DEVELOPER_TEAM_ID: ${{ secrets.DEVELOPER_TEAM_ID }}
          P12_PASSWORD: ${{ secrets.P12_PASSWORD }}
        run: |
          if [ -z "$BUILD_CERTIFICATE_BASE64" ]; then
            echo "has_cert=false" >> $GITHUB_OUTPUT
            echo "No certificate avalable... skipping" && exit 0
          else
            echo "has_cert=true" >> $GITHUB_OUTPUT
          fi
          # create variables
          CERTIFICATE_PATH=$RUNNER_TEMP/build_certificate.p12
          PP_PATH=$RUNNER_TEMP/FreeCAD_bundle.provisionprofile
          KEYCHAIN_PATH=$RUNNER_TEMP/app-signing.keychain-db

          export KEYCHAIN_PASSWORD=$(openssl rand -base64 8)

          # import certificate and provisioning profile from secrets
          echo -n "$BUILD_CERTIFICATE_BASE64" | base64 --decode -o $CERTIFICATE_PATH
          echo -n "$BUILD_PROVISION_PROFILE_BASE64" | base64 --decode -o $PP_PATH

          # create temporary keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security set-keychain-settings -lut 21600 $KEYCHAIN_PATH
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH

          # import certificate to keychain
          security import $CERTIFICATE_PATH -P "$P12_PASSWORD" -A -t cert -f pkcs12 -k $KEYCHAIN_PATH
          security set-key-partition-list -S apple-tool:,apple: -k "$KEYCHAIN_PASSWORD" $KEYCHAIN_PATH
          security list-keychain -d user -s $KEYCHAIN_PATH

          # apply provisioning profile
          mkdir -p ~/Library/Provisioning\ Profiles
          cp $PP_PATH ~/Library/Provisioning\ Profiles

          xcrun notarytool store-credentials "Parashell" --keychain "$KEYCHAIN_PATH" --apple-id "${APPLE_ID}" --password "${APP_SPECIFIC_PASSWORD}" --team-id "${DEVELOPER_TEAM_ID}"

      - name: Setup .NET 10 SDK
        if: runner.os == 'Windows'
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5.4.0
        with:
          dotnet-version: '10.0.103'

      - name: Install sign tool
        if: runner.os == 'Windows'
        run: |
          # NOTE: The sign tool is ONLY available as a prerelease package, they never issue real releases.
          dotnet tool install --global --prerelease sign
          echo "$env:USERPROFILE\.dotnet\tools" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          dotnet --info
          sign --version

      - name: Build packages
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_PLATFORM: ${{ matrix.target }}
          BUILD_TAG: ${{ needs.upload_src.outputs.build_tag }}
        run: |
          set -euo pipefail
          if [[ "${{ runner.os }}" == "macOS" ]]; then
            export MACOS_DEPLOYMENT_TARGET="${{ matrix.deploy_target }}"
          fi
          python3 package/scripts/write_version_info.py ../freecad_version.txt
          cd package/rattler-build
          pixi install --frozen

      - name: Azure login for Windows build code signing
        id: azure_login
        if: runner.os == 'Windows'
        continue-on-error: true
        uses: azure/login@532459ea530d8321f2fb9bb10d1e0bcf23869a43 # v3.0.0
        with:
          creds: '{"clientId":"${{ secrets.AZURE_CLIENT_ID }}","clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}","subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}","tenantId":"${{ secrets.AZURE_TENANT_ID }}"}'

      - name: Release packages with optional code-signing on Windows and macOS
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_PLATFORM: ${{ matrix.target }}
          MAKE_INSTALLER: "true"
          UPLOAD_RELEASE: "true"
          BUILD_TAG: ${{ needs.upload_src.outputs.build_tag }}
        run: |
          set -euo pipefail

          export MACOS_SIGN_RELEASE=0
          export WINDOWS_SIGN_RELEASE=0

          case "${RUNNER_OS}" in
            Windows)
              export WINDOWS_SIGN_RELEASE="${{ steps.azure_login.outcome }}"
              export WINDOWS_AZURE_ENDPOINT="${{ vars.AZURE_TRUSTED_SIGNING_ENDPOINT }}"
              export WINDOWS_AZURE_CERTIFICATE_PROFILE="${{ vars.AZURE_TRUSTED_SIGNING_CERTIFICATE_PROFILE }}"
              export WINDOWS_AZURE_SIGNING_ACCOUNT="${{ vars.AZURE_TRUSTED_SIGNING_ACCOUNT }}"

              # For good measure, normalize the azure_login result to 1 or 0
              if [[ "${WINDOWS_SIGN_RELEASE:-}" == "success" ]]; then
                export WINDOWS_SIGN_RELEASE=1
              else
                export WINDOWS_SIGN_RELEASE=0
              fi
              ;;

            macOS)
              export MACOS_SIGN_RELEASE="${{ steps.macos_get_cert.outputs.has_cert }}"
              export MACOS_SIGNING_KEY_ID="${{ secrets.SIGNING_KEY_ID }}"
              export MACOS_DEPLOYMENT_TARGET="${{ matrix.deploy_target }}"
              export SPARKLE_PUBLIC_ED_KEY="${{ vars.SPARKLE_PUBLIC_ED_KEY }}"
              export SPARKLE_ED_PRIVATE_KEY="${{ secrets.SPARKLE_ED_PRIVATE_KEY }}"
              ;;
          esac

          cd package/rattler-build
          pixi run -e package create_bundle

      ## Needed if running on a self-hosted runner:
      # - name: Clean up keychain and provisioning profile
      #   if: ${{ always() }}
      #   run: |
      #     security delete-keychain $RUNNER_TEMP/app-signing.keychain-db
      #     rm ~/Library/MobileDevice/Provisioning\ Profiles/build_pp.mobileprovision
